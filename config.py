"""
Configuration file defining default paths for datasets, model saving, and results output.
"""
# Path to the directory containing training datasets
base_datadir = 'dataset/'

# Directory where trained model checkpoints and logs will be saved
model_savedir = 'model_savedir'

# Directory where evaluation results (plots, metrics) will be stored
results_savedir = 'results'
